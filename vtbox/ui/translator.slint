export struct Translation {
    new: string,
    edit: string,
    delete: string,
    cancel: string,
    ok: string,
    setting: string,
    about: string,
    help: string,
    setting-btn: string,
    about-btn: string,
    help-btn: string,
    name: string,
    address: string,
    warning: string,
    delete-or-not: string,
    reset-or-not: string,
    send: string,
    clear: string,
    password-mismatch: string,
    input-password: string,
    please-login: string,
    flushing: string,

    name-label: string,
    status-label: string,
    online: string,
    offline: string,
    busy: string,
    idle: string,
    update-time: string,

    error: string,
    undownload: string,
    downloading: string,
    downloaded: string,
    download-failed: string,

    slow: string,
    normal: string,
    fast: string,
    customer: string,

    setting-dialog-ui: string,
    setting-dialog-ui-font-size: string,
    setting-dialog-ui-font-family: string,
    setting-dialog-ui-win-WH: string,
    setting-dialog-ui-win-W: string,
    setting-dialog-ui-win-H: string,
    setting-dialog-ui-language: string,

    setting-dialog-account: string,
    setting-dialog-account-max-feerate: string,
    setting-dialog-account-max-fee-amount: string,
    setting-dialog-account-max-send-amount: string,
    setting-dialog-account-delete: string,
    setting-dialog-account-show-mnemonic: string,
    setting-dialog-account-change-password: string,
    setting-dialog-account-recover: string,
    setting-dialog-account-skip-utxo-amount: string,

    setting-dialog-proxy: string,
    setting-dialog-proxy-disabled: string,
    setting-dialog-proxy-enabled: string,
    setting-dialog-proxy-type: string,
    setting-dialog-proxy-url: string,
    setting-dialog-proxy-port: string,

    no-data: string,
    no-message: string,

    btc-price: string,
    btc-per-byte-fee: string,

    tool-send: string,
    tool-receive: string,
    tool-activity: string,
    tool-address-book: string,

    send-receive-address: string,
    send-amount: string,
    send-feerate: string,
    send-max-feerate: string,

    address-book-header-name: string,
    address-book-header-address: string,
    address-book-add-item-dialog-title: string,
    address-book-qrcode-dialog-title: string,
    address-book-dialog-title: string,

    activity-header-time: string,
    activity-header-txid: string,
    activity-header-operate: string,
    activity-header-amount: string,
    activity-header-fee: string,
    activity-header-status: string,

    password-dialog-title: string,
    password-dialog-password: string,

    tx-detail-dialog-title: string,
    tx-detail-dialog-network: string,
    tx-detail-dialog-send-address: string,
    tx-detail-dialog-receive-address: string,
    tx-detail-dialog-send-amount: string,
    tx-detail-dialog-fee-amount: string,
    tx-detail-dialog-tx-hex: string,
    tx-detail-dialog-tx-raw: string,

    change-password-dialog-title: string,
    change-password-dialog-old-password: string,
    change-password-dialog-new-password: string,
    change-password-dialog-confirm-password: string,

    new-account-dialog-title: string,
    new-account-dialog-password: string,
    new-account-dialog-password-confirm: string,
    new-account-dialog-mnemonic: string,
    new-account-dialog-password-mismatch: string,
    new-account-dialog-failed: string,
    new-account-dialog-type-new: string,
    new-account-dialog-type-recover: string,
    new-account-dialog-type-recover-placeholder: string,

    show-mnemonic-dialog-title: string,

    tip-copy: string,
    tip-add: string,
    tip-delete: string,
    tip-send: string,
    tip-play: string,
    tip-clear: string,
    tip-stop: string,
    tip-reset: string,
    tip-save: string,
    tip-network: string,
    tip-main-network: string,
    tip-test-network: string,
    tip-qrcode: string,
    tip-logout: string,
    tip-address-book: string,
    tip-max: string,
    tip-new-mnemonic: string,
    tip-browser: string,

    tip-help: string,
    tip-about: string,
    tip-setting: string,
    tip-edit: string,
    tip-up: string,
    tip-down: string,
    tip-show: string,
    tip-hide: string,
    tip-flush: string,
    tip-zoom-out: string,
    tip-zoom-in: string,
}

export global Translator {
    public pure function translation(is-cn: bool) -> Translation {
        return {
            new: is-cn ? "新建" : "New",
            edit: is-cn ? "编辑" : "Edit",
            delete: is-cn ? "删除" : "Delete",
            cancel: is-cn ? "取消" : "Cancel",
            ok: is-cn ? "确定" : "Confirm",
            setting-btn: is-cn ? "设置" : "Setting",
            about-btn: is-cn ? "关于" : "About",
            help-btn: is-cn ? "帮助" : "Help",
            setting: is-cn ? "设     置" : "Setting",
            about: is-cn ?   "关     于" : "About",
            help: is-cn ?    "帮     助" : "Help",
            name: is-cn ?    "名     称" : "Name",
            address: is-cn ?    "地     址" : "Address",
            warning: is-cn ? "警     告" : "Warning",
            delete-or-not: is-cn ? "是否删除？" : "Are you sure to delete?",
            reset-or-not: is-cn ? "是否重置？" : "Reset or not?",
            send: is-cn ? "发送" : "Send",
            clear: is-cn ? "清空" : "Clear",
            password-mismatch: is-cn ? "密码错误" : "Old password and new password mismatch",
            input-password: is-cn ? "请输入密码" : "Please input password",
            please-login: is-cn ? "请登陆" : "Please login",
            flushing: is-cn ? "正在刷新..." : "Flush...",

            name-label: is-cn ? "昵称:" : "Name:",
            status-label: is-cn ? "状态:" : "Status:",
            online: is-cn ? "在线" : "Online",
            offline: is-cn ? "离线" : "Offline",
            busy: is-cn ? "正忙" : "Busy",
            idle: is-cn ? "空闲" : "Idle",
            update-time: is-cn ? "更新时间" : "Uptime",

            error: is-cn ? "出错" : "Error",
            undownload: is-cn ? "未下载" : "Undownload",
            downloading: is-cn ? "正在下载" : "Downloading",
            downloaded: is-cn ? "已下载" : "Downloaded",
            download-failed: is-cn ? "下载失败" : "Download failed",

            slow: is-cn ? "慢" : "slow",
            normal: is-cn ? "正常" : "normal",
            fast: is-cn ? "快" : "fast",
            customer: is-cn ? "自定义" : "customer",

            setting-dialog-ui: is-cn ? "界面" : "UI",
            setting-dialog-ui-font-size: is-cn ? "字体大小:" : "Font size:",
            setting-dialog-ui-font-family: is-cn ? "字体样式:" : "Font family:",
            setting-dialog-ui-win-WH: is-cn ? "窗口大小:" : "Window size:",
            setting-dialog-ui-win-W: is-cn ? "宽" : "Width",
            setting-dialog-ui-win-H: is-cn ?  "高" : "Height",
            setting-dialog-ui-language: is-cn ? "选择语言:" : "Language:",

            setting-dialog-account: is-cn ? "账户" : "Account",
            setting-dialog-account-max-feerate: is-cn ? "最大费率(sat/vB)" : "Max fee(sat/vB)",
            setting-dialog-account-max-fee-amount: is-cn ? "最大费用(sat)" : "Max fee(sat)",
            setting-dialog-account-max-send-amount: is-cn ? "最大发送数量(BTC)" : "Max send(BTC)",
            setting-dialog-account-delete: is-cn ? "删除账户    " : "Delete account",
            setting-dialog-account-show-mnemonic: is-cn ? "查看组记词" : "Show mnemonic",
            setting-dialog-account-change-password: is-cn ? "更改密码" : "Change password",
            setting-dialog-account-recover: is-cn ? "恢复账户" : "Recover account",
            setting-dialog-account-skip-utxo-amount: is-cn ? "跳过 UTXO(sat)" : "Skip UTXO(sat)",

            setting-dialog-proxy: is-cn ? "代理" : "Proxy",
            setting-dialog-proxy-type: is-cn ? "代理类型:" : "Type:",
            setting-dialog-proxy-port: is-cn ? "端口:" : "Port:",
            setting-dialog-proxy-disabled: is-cn ? "未启用代理" : "Disabled proxy",
            setting-dialog-proxy-enabled: is-cn ? "已启用代理" : "Enabled proxy",

            no-data: is-cn ? "没有数据" : "No data",
            no-message: is-cn ? "没有信息" : "No message",

            btc-price: is-cn ? "价格" : "Price",
            btc-per-byte-fee: is-cn ? "费率(sat/vB)" : "Fee(sat/vB)",


            tool-send: is-cn ? "发送" : "Send",
            tool-receive: is-cn ? "接收" : "Receive",
            tool-activity: is-cn ? "活动" : "Activity",
            tool-address-book: is-cn ? "地址簿" : "Address",

            send-receive-address: is-cn ? "接收地址" : "Address",
            send-amount: is-cn ? "发送数量(BTC)" : "Amount(BTC)",
            send-feerate: is-cn ? "费率(sat/vB)" : "Fee(sat/vB)",
            send-max-feerate: is-cn ? "最大费率" : "max fee rate",

            address-book-header-name: is-cn ? "名称" : "Name",
            address-book-header-address: is-cn ? "地址" : "Address",
            address-book-add-item-dialog-title: is-cn ? "添加地址" : "Add address",
            address-book-qrcode-dialog-title: is-cn ? "二维码" : "QrCode",
            address-book-dialog-title: is-cn ? "地址簿" : "Address book",

            activity-header-time: is-cn ? "时间" : "Time",
            activity-header-txid: is-cn ? "交易ID" : "TxId",
            activity-header-operate: is-cn ? "操作" : "Operate",
            activity-header-amount: is-cn ? "数量(BTC)" : "Amount",
            activity-header-fee: is-cn ? "费用(BTC)" : "Fee",
            activity-header-status: is-cn ? "状态" : "Status",

            password-dialog-title: is-cn ? "请输入密码" : "Please input password",
            password-dialog-password: is-cn ? "密  码" : "Password",

            tx-detail-dialog-title: is-cn ? "交易详情" : "Tx Detail",
            tx-detail-dialog-network: is-cn ? "网       络" : "Network",
            tx-detail-dialog-send-address: is-cn ? "发送地址" : "Send address",
            tx-detail-dialog-receive-address: is-cn ? "接受地址" : "Receive address",
            tx-detail-dialog-send-amount: is-cn ? "发送数量" : "Send amount",
            tx-detail-dialog-fee-amount: is-cn ? "交易费用" : "Fee amount",
            tx-detail-dialog-tx-hex: is-cn ? "交易细节(Hex)" : "Tx Detail(Hex)",
            tx-detail-dialog-tx-raw: is-cn ? "交易细节(Raw)" : "Tx Detail(Raw)",

            change-password-dialog-title: is-cn ? "更改密码" : "Change password",
            change-password-dialog-old-password: is-cn ? "旧  密  码" : "Old password",
            change-password-dialog-new-password: is-cn ? "新  密  码" : "New password",
            change-password-dialog-confirm-password: is-cn ? "重新输入" : "Confirm password",

            new-account-dialog-title: is-cn ? "创建用户" : "New account",
            new-account-dialog-password: is-cn ? "输入密码" : "Input password",
            new-account-dialog-password-confirm: is-cn ? "确认密码" : "Confirm password",
            new-account-dialog-mnemonic: is-cn ? "组记词" : "Mnemonic",
            new-account-dialog-password-mismatch: is-cn ? "新旧密码不同" : "Old password and new password mismatch",
            new-account-dialog-failed: is-cn ? "创建账户失败" : "New account failed",
            new-account-dialog-type-new: is-cn ? "新建账户" : "New Account",
            new-account-dialog-type-recover: is-cn ? "恢复账户" : "Recover account",
            new-account-dialog-type-recover-placeholder: is-cn ? "请输入24位组记词, 每个组记词使用空格分割" : "Please input 24 mnemonics and space between each mnemonic",

            show-mnemonic-dialog-title: is-cn ? "组记词" : "Mnemonic",

            tip-copy: is-cn ? "复制" : "Copy",
            tip-add: is-cn ? "添加" : "Add",
            tip-send: is-cn ? "发送" : "Send",
            tip-clear: is-cn ? "清除" : "Clear",
            tip-play: is-cn ? "播放" : "Play",
            tip-stop: is-cn ? "停止" : "Stop",
            tip-reset: is-cn ? "重置" : "Reset",
            tip-delete: is-cn ? "删除" : "Delete",
            tip-save: is-cn ? "保存" : "Save",
            tip-network: is-cn ? "网络" : "Network",
            tip-main-network: is-cn ? "主网络" : "Main network",
            tip-test-network: is-cn ? "测试网络" : "Test network",
            tip-qrcode: is-cn ? "二维码" : "QrCode",
            tip-logout: is-cn ? "登出" : "Logout",
            tip-address-book: is-cn ? "地址簿" : "Address book",
            tip-max: is-cn ? "最大数量" : "Max Value",
            tip-new-mnemonic: is-cn ? "重新生成组记词" : "Renew mnemonic",
            tip-browser: is-cn ? "浏览器" : "Browser",

            tip-help: is-cn ? "帮助" : "Help",
            tip-about: is-cn ? "关于" : "About",
            tip-setting: is-cn ? "设置" : "Setting",
            tip-edit: is-cn ? "编辑" : "Edit",
            tip-up: is-cn ? "向上" : "Move Up",
            tip-down: is-cn ? "向下" : "Move down",
            tip-show: is-cn ? "显示" : "Show",
            tip-hide: is-cn ? "隐藏" : "Hide",
            tip-flush: is-cn ? "刷新" : "Flush",
            tip-zoom-out: is-cn ? "缩小" : "Zoom Out",
            tip-zoom-in: is-cn ? "放大" : "Zoom In",
        };
    }
}
